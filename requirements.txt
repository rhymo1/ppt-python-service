# Flask App v5 - iSFP Data Extraction & PPT Generation
# Core
flask>=3.0,<4.0
gunicorn>=21.2,<23.0

# PDF extraction (Bug #10: pdfplumber replaces PyPDF2)
pdfplumber>=0.10,<1.0

# PowerPoint generation
python-pptx>=0.6.23,<1.0

# Image handling
Pillow>=10.0,<11.0

# XML parsing (for PPT blipFill manipulation)
lxml>=4.9,<6.0

# Charts
matplotlib>=3.8,<4.0
numpy>=1.24,<3.0

# PDF image extraction (Priority 3: page rendering + auto-crop)
PyMuPDF>=1.23,<2.0

# OCR fallback (optional — install separately if needed)
# pytesseract>=0.3.10,<1.0
# Also requires system package: tesseract-ocr tesseract-ocr-deu
